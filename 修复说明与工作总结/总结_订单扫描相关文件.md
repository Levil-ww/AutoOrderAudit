# AutoOrderAudit 订单扫描相关文件总结

## 一、核心扫描流程

### 1.1 扫描架构图
```
┌─────────────────────────────────────────────────────────────────┐
│                      自动审单扫描流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────────┐  │
│  │  GUI 入口    │ →  │  Engine 引擎  │ →  │  Adapter 适配器     │  │
│  │  (gui.py)   │    │  (engine.py) │    │  (adapter.py)      │  │
│  └─────────────┘    └─────────────┘    └─────────────────────┘  │
│         ↓                   ↓                   ↓                │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Parser 解析器 (parser.py)                 │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 关键扫描步骤

| 步骤 | 模块 | 函数 | 说明 |
|------|------|------|------|
| 1. 查询订单 | adapter.py | `query_orders()` | 调用方果API拉取待整理订单列表 |
| 2. 补充详情 | adapter.py | `_enrich_order_with_detail()` | 补充备注、商品行、快递编码等信息 |
| 3. 解析备注 | parser.py | `extract_multiple_remarks()` | 解析卖家备注，提取编码信息 |
| 4. 审单处理 | engine.py | `_process_order()` | 订单分类处理（普通/合并/补差价） |
| 5. 更新编码 | adapter.py | `update_merchant_code()` | 更新商品行商家编码 |
| 6. 验证结果 | adapter.py | `is_already_correct()` | 验证编码是否已正确 |

---

## 二、扫描相关文件详解

### 2.1 主入口文件

#### [main.py](file:///d:/AutoOrderAudit/main.py)
- **功能**: 程序主入口
- **职责**: 初始化适配器、引擎，启动审单流程
- **关键配置**: `DRY_RUN` 模式开关、`max_orders` 最大处理订单数

#### [gui.py](file:///d:/AutoOrderAudit/gui.py)
- **功能**: 图形界面入口
- **职责**: 提供用户交互界面，显示处理进度和结果
- **新增功能**: 
  - 「🔄 自动监控」按钮：开启/关闭自动轮询
  - 自适应间隔算法：有订单时10秒，无订单时逐步拉长至180秒
  - Token过期自动检测

#### [run_gui.py](file:///d:/AutoOrderAudit/run_gui.py)
- **功能**: GUI启动入口

---

### 2.2 引擎层 (Engine)

#### [engine.py](file:///d:/AutoOrderAudit/core/engine.py)
- **功能**: 自动审单引擎核心
- **职责**: 
  - 协调订单查询、解析、修改流程
  - 分类处理不同类型订单
  - 统计处理结果（成功/跳过/失败）

- **关键方法**:

| 方法 | 说明 |
|------|------|
| `run()` | 执行完整审单流程 |
| `run_monitor_cycle()` | 轻量级监控周期（自动监控模式使用） |
| `_process_order()` | 订单分类处理入口 |
| `_process_normal_order_logic()` | 普通订单处理 |
| `_process_merged_order()` | 合并订单处理 |
| `_process_price_difference_order()` | 补差价订单处理 |

- **订单分类逻辑**:
```python
if 订单号包含 '&' 或 商品行有不同的 original_tid:
    走合并订单处理
elif 订单只有补差价商品行:
    走补差价订单处理
else:
    走普通订单处理
```

- **跳过关键字列表** (`_SKIP_KEYWORDS`):
  - "待定"、"待确定"、"待确认"
  - "等通知发"、"需要效果图"
  - "正常发"、"仓库发"、"工厂发"
  - "补发"、"已代益"、"已代森"

---

### 2.3 适配器层 (Adapter)

#### [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)
- **功能**: 方果ERP适配器
- **职责**: 
  - 实现 `ErpAdapter` 接口
  - 调用方果API进行订单操作
  - 处理商品行编码逻辑

- **关键方法**:

| 方法 | 说明 |
|------|------|
| `query_orders()` | 查询待整理订单 |
| `update_merchant_code()` | 更新商品行编码（核心方法） |
| `is_already_correct()` | 验证编码是否已正确 |
| `_enrich_order_with_detail()` | 补充订单详情 |
| `_is_gift_item()` | 判断是否为赠品行 |
| `_is_price_difference_item()` | 判断是否为补差价行 |

- **商品行处理四步法**:
  1. **第一步**: 创建/更新普通商品行（基于解析结果）
  2. **第二步**: 处理赠品（更新或创建赠品行）
  3. **第三步**: 保留未匹配的非作废、非赠品商品行
  4. **第四步**: 保留作废商品行

---

### 2.4 解析器层 (Parser)

#### [parser.py](file:///d:/AutoOrderAudit/core/parser.py)
- **功能**: 卖家备注解析引擎
- **职责**: 
  - 解析自然语言备注
  - 提取材质、尺寸、花型、赠品等信息
  - 生成标准编码格式

- **核心数据结构**:
```python
class ParsedRemark:
    material_code: str      # 材质编码
    color_code: str         # 颜色/类型（定制/标准）
    model_code: str         # 型号（尺寸或裁剪类型）
    picture_code: str       # 花型;尺寸
    num: int                # 数量
    gift_name: str          # 赠品名称
    gift_num: int           # 赠品数量
    gifts: list             # 多个赠品列表
    is_stock: bool          # 是否现货
    original_tid: str       # 原始订单号（合并订单用）
    shop_remark: str        # 商品行级备注
```

- **关键方法**:

| 方法 | 说明 |
|------|------|
| `parse_remark()` | 解析单条备注 |
| `extract_multiple_remarks()` | 解析多条备注（一单多商品） |
| `_split_into_segments()` | 多商品分割（三级优先级） |
| `_extract_all_sizes()` | 提取所有尺寸 |
| `_parse_material()` | 三级材质提取 |
| `_extract_pattern()` | 提取花型名称 |
| `_extract_gift()` | 提取赠品信息 |
| `_extract_multiple_gifts()` | 提取多个赠品 |

- **编码格式**:
  - 定制: `材质-定制-裁剪有图/定制尺寸-花型;尺寸CM`
  - 现货: `材质-标准-尺寸-花型;尺寸` (无CM单位)

---

### 2.5 基类定义

#### [adapter_base.py](file:///d:/AutoOrderAudit/core/adapter_base.py)
- **功能**: ERP适配器抽象基类
- **职责**: 定义通用接口和数据模型

- **核心类**:
  - `ErpAdapter`: 适配器接口
  - `Order`: 订单数据模型
  - `OrderItem`: 商品行数据模型

- **OrderItem 关键字段**:
```python
class OrderItem:
    id: str                  # 商品行ID
    order_id: str            # 订单ID
    oid: str                 # 子订单号
    title: str               # 商品标题
    shop_mapping_sku: str    # 商家编码
    num: int                 # 数量
    price: float             # 价格
    is_void: bool            # 是否已作废
    raw: dict                # 原始数据
    shop_remark: str         # 商品行级备注
    original_tid: str        # 原始订单号
    type: int                # 类型（0=原始商品, 1=手工单）
```

---

## 三、扫描流程详解

### 3.1 普通订单扫描流程
```
订单查询 → 备注解析 → 编码匹配 → 编码验证 → API更新
    ↓           ↓           ↓           ↓
  query_    parse_remark  is_already_  update_
  orders()  extract_      correct()    merchant_
            multiple_                  code()
            remarks()
```

### 3.2 合并订单扫描流程
```
订单查询 → 检测&符号 → 按original_tid分组 → 各组独立处理
    ↓           ↓              ↓              ↓
  query_    _process_      分组处理       每组独立解析
  orders()  merged_order                 + 修改编码
```

### 3.3 自动监控扫描流程
```
GUI启动 → 开启监控 → 循环检测 → 有订单则处理 → 自适应等待
    ↓          ↓          ↓          ↓          ↓
  用户点击  监控线程   query_orders  run()/   sleep(自适应)
            启动                 engine.run_monitor_cycle()
```

### 3.4 自适应间隔算法
| 订单数量 | 延迟时间 | 说明 |
|----------|----------|------|
| ≥20 | 10秒 | 订单量大，快速跟进 |
| 10-19 | 21秒 | 中等数量 |
| 5-9 | 30秒 | 正常标准 |
| 2-4 | 39秒 | 少量订单 |
| 1 | 60秒 | 单个订单 |
| 0 | 30s→180s | 逐步拉长 |

---

## 四、配置文件

### 4.1 [config.py](file:///d:/AutoOrderAudit/adapters/fangguo/config.py)
- **功能**: 方果ERP适配器配置
- **关键配置**:
  - `API_QUERY_ORDER`: 订单查询API地址
  - `API_SAVE_PRODUCT`: 保存商品API地址
  - `AUTHORIZATION`: 鉴权Token
  - `COOKIE_STR`: 会话Cookie
  - `TENANT_ID`: 租户ID
  - `DRY_RUN`: 模拟模式开关
  - `MATERIAL_MAP`: 静态材质映射表

### 4.2 [express_config.py](file:///d:/AutoOrderAudit/express_config.py)
- **功能**: 快递配置
- **关键配置**:
  - 快递公司编码映射
  - 区域-快递匹配规则

### 4.3 [auth_manager.py](file:///d:/AutoOrderAudit/auth_manager.py)
- **功能**: Token自动管理
- **职责**: 
  - 检测Token有效性
  - 自动刷新过期Token
  - 7天滚动有效期管理

---

## 五、文件关系图

```
AutoOrderAudit/
├── main.py                    # 主入口
├── run_gui.py                 # GUI入口
├── gui.py                     # 图形界面
├── auth_manager.py            # Token管理
├── express_config.py          # 快递配置
│
├── core/                      # 核心逻辑
│   ├── adapter_base.py        # 基类定义
│   ├── engine.py              # 审单引擎
│   └── parser.py              # 备注解析
│
├── adapters/                  # ERP适配器
│   └── fangguo/
│       ├── adapter.py         # 方果适配器
│       ├── config.py          # 方果配置
│       └── material_source.py # 材质数据源
│
└── Test/                      # 测试文件
    ├── test_duplicate_order_fix.py
    ├── test_gift_fix.py
    ├── test_price_difference_order.py
    └── ...
```

---

## 六、常用扫描场景

### 6.1 定时扫描（手动触发）
```python
# 直接运行审单
python main.py

# 指定参数
python main.py --days 7 --dry-run True
```

### 6.2 自动监控扫描
```python
# 启动GUI后点击「自动监控」
# 程序自动轮询处理新订单
```

### 6.3 模拟模式扫描
```python
# 设置 DRY_RUN = True
# 只打印新编码，不实际提交API
```

---

## 七、参考文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 核心功能说明 | [核心功能说明.txt](file:///d:/AutoOrderAudit/核心功能说明.txt) | 整体架构和功能说明 |
| 自动监控说明 | [自动监控功能说明.txt](file:///d:/AutoOrderAudit/自动监控功能说明.txt) | 监控模式详细说明 |
| 程序介绍 | [程序简单介绍.txt](file:///d:/AutoOrderAudit/程序简单介绍.txt) | 项目概述 |
| Bug修复说明 | [语法检查与bug修复说明.txt](file:///d:/AutoOrderAudit/语法检查与bug修复说明.txt) | Bug修复记录 |
