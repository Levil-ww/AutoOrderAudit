# AutoOrderAudit 其他问题修复总结

## 一、备注解析相关修复

### 1.1 "正常发"跳过指令识别
- **问题**: 备注为"正常发"时，程序仍尝试修改商家编码
- **修改**: 将"正常发"加入 `_SKIP_KEYWORDS` 列表
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py)

### 1.2 圆形尺寸分割
- **问题**: 备注"都是蔓生花，定制双面格尺寸87cm和133cm各一张"无法正确解析为两个独立商品行
- **修改内容**:
  - 增强圆形尺寸识别，支持"尺寸"前缀
  - 多圆形尺寸用"和/与/、"分割，继承公共前缀
  - 圆形尺寸存在时用完整text提取花型
  - 处理圆形尺寸在body与text间的位置偏移
  - 清理中文数量词（"各一张"、"一张"）
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py)

### 1.3 编码格式判断缺少分号校验
- **问题**: `parse_remark` 的"情况1"只检查 `len(parts) >= 4`，没有检查最后一段是否包含分号
- **后果**: 像 `双面格-标准-33x120-塞纳时光`（无分号）这种非编码文本会被误判为编码格式
- **修改**: 在情况1的条件中增加 `";" in parts[-1]` 校验
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py)

### 1.4 圆形现货尺寸识别
- **问题**: 识别现货编码时，正则只匹配矩形尺寸（数字x数字），不匹配圆形尺寸
- **后果**: 圆形现货尺寸（如 `圆直径80`）不会被识别为现货
- **修改**: 新增圆形尺寸正则 `^(?:圆|圆形|圆直径|直径)\d+(?:\.\d+)?$`
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py)

### 1.5 现货编码格式支持
- **问题**: 现货订单的编码格式与定制不同，需要特殊处理
- **修改内容**:
  - `ParsedRemark` 类新增 `is_stock: bool` 字段
  - 现货编码格式：`材质-标准-尺寸-花型;尺寸`（无cm/CM单位）
  - 同组后续尺寸继承时，会自动加上"现货"前缀
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py)

---

## 二、合并订单相关修复

### 2.1 合并订单手工单携带备注
- **问题**: 合并订单生成的手工单携带长备注，导致编码错误
- **根因**:
  - `engine.py` 补差价分组解析结果 `shop_remark` 未显式设为空
  - `adapter.py` 中 `_build_order_item` 方法回退使用订单级备注
- **修改**:
  - 补差价行处理时强制清空 `shopRemark`
  - 新建手工单仅保留编码和数量，不携带备注
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 2.2 三层保护机制（手工单反复增值）
- **问题**: 方果ERP自动关联订单级备注到所有商品行（包括手工单），导致反复增值
- **修改**:
  - 第一层: 合并订单分组收集备注时跳过手工单行 (`type == 1`)
  - 第二层: 补差价分组优先使用自身备注
  - 第三层: 新建手工单提交时 `shopRemark` 为空
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py)

### 2.3 赠品备注覆盖普通商品行
- **问题**: ERP自动将订单级备注复制到赠品行，遍历时赠品行靠后的备注会覆盖分组普通商品行的真正备注
- **修改**: 在收集分组备注时增加 `not self.adapter._is_gift_item(item)` 判断，跳过赠品行作为备注来源
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py)

### 2.4 合并订单按original_tid匹配
- **问题**: 合并订单处理时，解析结果和商品行之间缺少 `original_tid` 的关联，导致按顺序匹配时出现错位
- **修改**:
  - `parser.py`: `ParsedRemark` 类添加 `original_tid` 字段
  - `adapter.py`: 修改 `update_merchant_code` 方法的匹配逻辑，按 `original_tid` 匹配商品行
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 2.5 合并订单中补差价行处理
- **问题**: 合并订单的某个分组是补差价分组且分组内没有普通商品行时，不会创建任何 `price_diff_updates`
- **修改**: 在 `if regular_items_in_group:` 前补充 `else` 分支，处理分组内没有普通商品行、只有补差价行的情况
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py)

### 2.6 合并订单赠品创建
- **问题**: 合并订单中子订单的赠品创建存在问题
- **修改**:
  - 修复赠品提取逻辑：当"送"在文本开头时，`prev_char` 是空字符串导致被误判
  - 修复赠品名称处理：当赠品名称就是"赠品"时，去掉前2个字符后变成空字符串
  - 增加保留未匹配的非作废、非赠品商品行的逻辑
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

---

## 三、赠品处理逻辑修复

### 3.1 赠品材质继承错误
- **问题**: 赠品材质本应是"吸水皮革"，却继承了商品的"双面格"材质
- **根因**: 赠品信息被挂到第一个商品段导致继承错误材质
- **修改**: 所有赠品 `effective_material_code` 强制固定为"吸水皮革"
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 3.2 赠品类型映射
- **修改**: 新增 `_GIFT_TYPE_MAP` 映射表

| 类型 | title | 商品ID | SKUID |
|------|-------|--------|-------|
| 圆垫/默认 | 赠品沥水垫小圆或小方 | 3806984491654840639 | 3644348072270850 |
| 方垫/30x50 | 赠品沥水垫30x50cm | 3806985099191386298 | 3644313731257858 |

- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 3.3 赠品title格式修正
- **问题**: 赠品title显示为"吸水皮革-标准-赠品沥水垫..."的手工单样式
- **修改**:
  - 新建赠品行: title 固定取 `gift_info['title']`
  - 已有赠品行: 保留原标识，更新标题
  - 移除旧的 `sourceSysOid` 和 `isCopy` 复制逻辑
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 3.4 赠品行type字段调整
- **问题**: 赠品行在方果Web端显示异常，无法删除
- **修改**（最终方案）:
  - `type=1`（手工单类型，前端显示删除按钮）
  - 继承 `sourceSysOid`（模板子订单号）
  - 标记 `isCopy=True`
  - `id` 和 `sysOid` 为 `None`
  - 设置 `originalGoodsId`、`originalSkuId`
  - 保留 `outerIid` 实现订单关联
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 3.5 赠品行被误删/覆盖
- **问题**:
  - 当备注中没有赠品信息时，现有赠品行被完全忽略，导致赠品行丢失
  - 创建新赠品行时覆盖了现有的商品行
- **修改**:
  - 添加逻辑：如果备注中没有赠品信息，但订单中存在赠品行，则保留这些赠品行不变
  - 增加 `is_new` 参数：创建新赠品行时标识字段置空，ERP会创建新行
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 3.6 多赠品支持
- **问题**: 备注包含多个赠品（如"送赠品圆垫-2张，赠品方垫-1张"）时无法正确处理
- **修改**:
  - 改造 `ParsedRemark` 类，添加 `gifts` 属性支持多个赠品
  - 改造 `_extract_gift()` 函数，返回多个赠品列表
  - 改造 `update_merchant_code()` 中的赠品循环处理逻辑
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 3.7 多赠品去重逻辑
- **问题**: 相同赠品重复创建
- **修改**:
  - 同一子订单内有相同赠品 → 合并为1个赠品行，数量累加
  - 不同子订单有相同赠品 → 分别创建独立的赠品行，不合并
- **文件**: [parser.py](file:///d:/AutoOrderAudit/core/parser.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

---

## 四、补差价订单处理修复

### 4.1 补差价订单编码逻辑
- **问题**: 补差价订单的编码处理逻辑不完善
- **修改内容**:
  - 移除了"差价"从跳过关键字列表
  - 添加了 `_is_price_difference_order` 方法检测补差价订单
  - 添加了 `_is_price_difference_item` 方法判断单个商品行
  - 支持合并订单中同时处理普通商品和补差价商品
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 4.2 补差价订单编码识别
- **问题**: 补差价商品行未被正确识别
- **修改**: 增加三种补差价检测方式：
  - SKU 为"定制-定制-补差价-不打印"
  - 备注为"差价"或"补差价"
  - 备注包含"差价不发货"、"不打印"、"不用发"等特征
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 4.3 混合订单补差价行处理
- **问题**: 混合订单（有普通商品行 + 补差价行）中补差价行完全没有被处理
- **修改**:
  - 备注为空时，如果订单中有补差价行，不再跳过，继续处理
  - 当外部未传入 `price_diff_updates` 时，自动检测订单中的补差价行并构建
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py)

### 4.4 补差价订单幂等优化
- **问题**: 补差价订单重复修改
- **修改**:
  - `update_price_difference_order` 方法添加编码正确性检查逻辑
  - 已正确时返回 `None` 表示跳过
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

---

## 五、赠品不送逻辑修复

### 5.1 "赠品不送"处理流程
- **处理流程**:
  1. 检测"赠品不送"：在引擎层面检测备注是否包含该关键词
  2. 允许空解析继续：即使无商品定制信息也会继续处理
  3. 赠品行匹配修改：将赠品行编码改为"定制-定制-补差价-不打印"
  4. 无赠品行则跳过
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py), [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

---

## 六、其他功能增强

### 6.1 确认回调机制
- **功能**: 新增参数 `confirm_callback`，在修改前弹窗确认
- **签名**: `confirm_callback(order, parsed_list, changes: list[str]) -> bool`
- **返回值**:
  - `True` → 继续执行 `update_merchant_code` API 修改
  - `False` → 仅日志记录，不调 API
- **文件**: [engine.py](file:///d:/AutoOrderAudit/core/engine.py), [gui.py](file:///d:/AutoOrderAudit/gui.py)

### 6.2 快递编码更新逻辑
- **功能**: 程序只会在快递不符合规则时才修改
- **增强**:
  - 增加了更多字段名尝试，确保能正确提取当前快递编码
  - 完善调试日志，显示当前快递编码
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py), [engine.py](file:///d:/AutoOrderAudit/core/engine.py)

### 6.3 自动监控功能
- **功能**: 实现了智能自适应间隔轮询机制
- **核心改进**:
  - 智能延迟等待（检测到订单后等待30~60秒让客服完善备注）
  - 动态延迟计算（根据订单数量自动调整）
  - API调用冷却（每次调用间隔至少5秒）
- **文件**: [gui.py](file:///d:/AutoOrderAudit/gui.py), [engine.py](file:///d:/AutoOrderAudit/core/engine.py)

### 6.4 新建商品行type字段
- **问题**: 新建的商品行不显示"删除商品"按钮
- **修改**: 新建商品行的 `type` 字段从 `0` 改为 `1`，使其被识别为手工单
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

### 6.5 数量提取修复
- **问题**: 商品数量使用的是原始订单中的数量，而不是通过解析出的数量
- **修改**: 将 `_build_order_item` 中的 `num: item.num` 改为 `num: parsed.num`
- **文件**: [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py)

---

## 七、修复文件汇总

| 文件 | 主要修改类型 | 修改次数 |
|------|------------|---------|
| [parser.py](file:///d:/AutoOrderAudit/core/parser.py) | 备注解析、编码格式、赠品提取 | 20+ |
| [engine.py](file:///d:/AutoOrderAudit/core/engine.py) | 订单处理、合并订单、补差价 | 15+ |
| [adapter.py](file:///d:/AutoOrderAudit/adapters/fangguo/adapter.py) | 商品行构建、赠品处理、编码验证 | 25+ |
| [gui.py](file:///d:/AutoOrderAudit/gui.py) | 监控功能、确认回调 | 5+ |
| [adapter_base.py](file:///d:/AutoOrderAudit/core/adapter_base.py) | 基类方法签名添加参数 | 2 |

---

## 八、测试覆盖

### 8.1 新增测试文件

| 文件 | 说明 |
|------|------|
| [test_duplicate_order_fix.py](file:///d:/AutoOrderAudit/test_duplicate_order_fix.py) | 重复订单检测和修复测试 |
| [test_manual_item_red_tag.py](file:///d:/AutoOrderAudit/test_manual_item_red_tag.py) | 手工单行识别测试 |
| [test_price_difference_order.py](file:///d:/AutoOrderAudit/Test/test_price_difference_order.py) | 补差价订单处理测试 |

### 8.2 测试场景统计
- 补差价订单测试：11个场景
- 去重逻辑测试：7个场景
- 编码正确时跳过测试
- 赠品处理测试（多个场景）

---

## 九、待处理事项

1. 合并订单分组键可能含 `&` 符号处理
2. `parser.py` 中冗余未使用函数 `_extract_gift` 清理
3. `test_multi_gift.py` 中4个失败用例排查
4. 方果ERP赠品最终效果需实际订单验证
