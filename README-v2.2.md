# AutoOrderAudit V2.2 — 自动审单系统

基于卖家备注自动解析并修改 ERP 订单商品编码的自动化工具，专为电商客服场景设计。

## 项目概述

AutoOrderAudit 连接方果 ERP 系统，自动拉取待整理订单，解析卖家备注中的**材质-颜色-尺寸-花型**编码，更新订单商品商家编码（shop_mapping_sku），大幅减少客服人工处理订单的时间。

核心流程：

```
方果ERP ──[API]──→ 适配器拉取订单 ──→ 解析卖家备注 ──→ 生成商家编码 ──→ [API]更新回ERP
                                       │
                                       ├─ 自动更新快递（备注关键词/省份规则）
                                       ├─ 补差价订单处理
                                       ├─ 合并订单拆分处理
                                       ├─ 赠品检测与编码生成
                                       └─ 重复商品行自动清理
```

## 功能特性

### 核心功能
- **自动审单** — 批量拉取待整理订单，逐单解析备注并更新编码
- **智能备注解析** — 支持编码格式（`材质-颜色-尺寸-花型`）和自然语言备注的混合解析
- **材质同义词映射** — 内置同义词映射表，兼容客服不同写法（如"双面芊"→"双面格"）
- **材质自动匹配** — 从方果 API 拉取材质列表，支持模糊匹配（`difflib.SequenceMatcher`），识别率高
- **合并订单处理** — 识别合并订单（订单号含 `&` 或多个 `original_tid`），按商品行级备注逐一分配到子订单
- **补差价订单处理** — 自动识别补差价商品行，支持多种场景（不发货、备注含定制信息、混合订单等）
- **赠品检测与编码** — 自动识别备注中的赠品信息，支持多赠品、圆垫/方垫/默认三种规格
- **安全关键字过滤** — 备注含"待定"、"等通知发"等关键字时自动跳过
- **多尺寸拆分** — 同一备注中多个尺寸自动拆分为独立商品行，支持材质和花型继承
- **重复商品行清理** — 全局检测并清理重复商品行（按标准化 SKU+数量 分组），避免重复创建
- **二次运行幂等校验** — 编码+数量双重校验，已正确的订单自动跳过

### V2.2 新增功能
- **全局重复行清理** — 扫描非取消商品行，按标准化 SKU+数量 分组，每组仅保留一行，其余标记为取消
- **price=0 商品防误识别** — 改进 `_is_gift_item` 逻辑，通过 SKU 格式（3+ `-` 分隔符）区分正常商品与赠品，避免 price=0 的正常商品被误判为赠品导致重复创建
- **SKU 模糊匹配** — 精确匹配失败时（CM 单位差异、尾部备注差异），使用标准化 SKU 子集进行最佳分数匹配
- **多赠品支持** — 备注可同时包含多个赠品（如"送圆垫-2张，方垫-1张"），按 original_tid 分组独立处理
- **商品行/赠品行去重** — 同一子订单内相同 SKU 合并数量，不同子订单独立保留
- **混合订单补差价处理** — 普通订单与补差价行共存时，解析结果自动分配，补差价行数量同步修正
- **"赠品不送"支持** — 备注含"赠品不送"时，自动将赠品行编码改为"定制-定制-补差价-不打印"
- **补差价关键词扩展** — 支持"差价"、"不打印"、"不用发"等多种不发货触发条件
- **新建手工单可删除** — 新建商品行/赠品行 type=1，方果界面显示"删除商品"按钮
- **合并订单检测顺序优化** — 先检测合并订单，再检测补差价订单，确保分组逻辑正确
- **快递更新优化** — 仅在当前快递不符合规则时才修改，增强编码字段兼容性
- **GUI 日志精简** — 移除收件人详细地址、完整 JSON 结构等敏感/冗余信息
- **数量正确性校验** — 二次运行时同时校验 SKU 和数量，数量错误也会触发更新

### V2.1 功能（已包含）
- **自动监控模式** — 自适应间隔轮询待处理订单，有订单时快查（10秒），无订单时逐步拉长至3分钟
- **快递自动配置** — 根据备注关键词或收件省份自动更新订单快递（优先级：备注关键词 > 省份规则）
- **快递配置 GUI** — 可视化编辑省份-快递映射和快递类型
- **Token 自动管理** — 账号密码自动登录，Token 过期自动检测并弹窗提醒重新登录
- **退出登录** — 支持清除 Token 退出登录
- **Dry Run 模式** — 仅打印不提交，安全测试
- **图形界面** — 基于 tkinter 的美化版 GUI，适合非技术人员直接使用

## 目录结构

```
AutoOrderAudit/
├── adapters/                          # 适配器模块（可扩展多个ERP）
│   └── fangguo/                       # 方果ERP适配器
│       ├── __init__.py                # 包入口，导出 FangguoAdapter 及配置
│       ├── adapter.py                 # 方果ERP适配器实现（查询订单、更新编码、更新快递、重复清理）
│       ├── config.py                  # 方果配置（API地址、材质映射、Token热加载）
│       ├── material_source.py         # 材质数据源（从API拉取材质列表，模糊匹配）
│       └── ExampleFangguo.txt         # 示例/模板文件
│
├── core/                              # 核心引擎模块（通用，不依赖具体ERP）
│   ├── __init__.py                    # 包入口，导出核心类
│   ├── adapter_base.py                # ERP适配器抽象接口基类（Order/OrderItem/ErpAdapter）
│   ├── engine.py                      # 自动审单引擎（核心流程编排、合并订单、补差价、快递更新）
│   └── parser.py                      # 卖家备注解析引擎（正则+规则、多尺寸拆分、赠品提取、去重）
│
├── Test/                              # 测试模块
│   ├── test_arrival_refund.py         # 到货返金额过滤测试
│   ├── test_deduplication.py          # 商品行去重测试
│   ├── test_duplicate_order_fix.py    # 重复订单修复测试
│   ├── test_gift_extract.py           # 赠品提取测试
│   ├── test_gift_fix.py               # 赠品修复测试
│   ├── test_gift_format.py            # 赠品格式化测试
│   ├── test_gift_pad_format.py        # 赠品沥水垫格式测试
│   ├── test_manual_item_red_tag.py    # 手工单红标测试
│   ├── test_manual_type.py            # 手工单类型测试
│   ├── test_multi_gift.py             # 多赠品测试
│   ├── test_only_gift.py              # 仅赠品订单测试
│   ├── test_order_scenario.py         # 订单场景测试
│   ├── test_price_difference_order.py # 补差价订单测试
│   ├── test_red_color.py              # 红色标识测试
│   ├── test_skip_if_correct.py        # 编码已正确跳过测试
│   ├── test_stock_goods.py            # 现货商品测试
│   └── revert_order.py                # 订单回滚工具
│
├── auth_client.py                     # 方果登录客户端（封装登录API、二次租户登录）
├── auth_manager.py                    # Token管理（自动登录/保存/刷新/过期检测/回调通知）
├── express_config.py                  # 快递配置模块（省份规则、备注关键词、可视化编辑）
├── express_config.json                # 快递配置存储文件
├── gui.py                             # 图形界面（tkinter美化版、自动监控、快递配置对话框）
├── run_gui.py                         # GUI启动脚本（客服双击运行）
├── main.py                            # 命令行入口
├── test.py                            # 测试入口脚本
├── token.json                         # Token存储文件（程序自动管理）
├── requirements.txt                   # Python依赖列表
├── README.md                          # 项目说明文档
└── README-v2.2.md                     # V2.2 版本说明文档
```

## 技术架构

### 分层设计

| 层 | 模块 | 职责 |
|---|---|---|
| **适配器层** | `adapters/fangguo/` | 实现 `ErpAdapter` 接口，与具体 ERP 系统通信（查单、改码、更新快递、重复清理） |
| **核心引擎层** | `core/engine.py` | 编排审单流程：合并订单拆分、补差价处理、快递更新、确认回调、幂等校验 |
| **解析引擎** | `core/parser.py` | 通用备注解析：正则提取、多尺寸拆分、材质/花型继承、赠品检测、去重 |
| **认证层** | `auth_client.py` + `auth_manager.py` | Token 自动获取、生命周期管理、过期检测、回调通知 |
| **快递配置层** | `express_config.py` + `express_config.json` | 省份规则、备注关键词映射、快递类型管理 |
| **界面层** | `gui.py` | 桌面图形界面、自动监控、快递配置对话框、Token过期弹窗 |

### 关键抽象

- **ErpAdapter**（`core/adapter_base.py`） — ERP 适配器抽象接口。所有 ERP 系统实现该接口即可接入引擎。
- **Order / OrderItem**（`core/adapter_base.py`） — 通用订单/商品行数据模型，包含 `original_tid`、`shop_remark`、`receiver_province`、`current_cp_code`、`is_void` 等字段。
- **ParsedRemark**（`core/parser.py`） — 备注解析结果模型，包含 `material_code`、`color_code`、`model_code`、`picture_code`、`is_stock`、`gifts`、`original_tid`、`shop_remark` 等字段。
- **AutoAuditEngine**（`core/engine.py`） — 自动审单引擎，遍历订单 → 解析备注 → 更新编码 → 更新快递，支持回调确认、自动监控、跳过缓存。

### 处理流程

```
订单进入引擎
  │
  ├─ 检测合并订单（订单号含& 或 多个original_tid）
  │    └─ 按 original_tid 分组，每组使用商品行级备注独立处理
  │
  ├─ 检测补差价订单
  │    ├─ 备注为空/"补差价"/"差价不发货" → 编码改为"定制-定制-补差价-不打印"
  │    ├─ 仅有补差价行 + 备注含定制信息 → 按正常解析修改编码
  │    └─ 混合订单 → 解析结果分配给普通行，剩余分配给补差价行
  │
  ├─ 普通订单处理
  │    ├─ 安全关键字检测（待定、等通知发...） → 跳过
  │    ├─ 解析卖家备注 → 生成编码
  │    ├─ 赠品检测 → 生成赠品行（支持多赠品、赠品不送）
  │    ├─ 重复商品行清理 → 标准化 SKU+数量 分组去重
  │    └─ "赠品不送" → 赠品行编码改为不发货编码
  │
  └─ 更新快递（所有订单通用）
       ├─ 优先：备注关键词匹配（"发极兔"、"发中通"等）
       └─ 其次：收件省份规则匹配
```

## 备注解析规则

### 编码格式

| 类型 | 编码格式 | 示例 |
|---|---|---|
| **定制** | `材质-定制-定制尺寸/裁剪有图-花型;尺寸CM` | `双面格-定制-裁剪有图-花满金陵;100x150CM` |
| **现货** | `材质-标准-尺寸-花型;尺寸`（无cm/CM单位） | `双面格-标准-100x150-花满金陵;100x150` |

### 自然语言格式

支持多种自然语言写法：

- `灰色双面格1张100x150花满金陵`
- `双面格灰色100x150花满金陵1个`
- `圆80cm 双面格 灰色 花满金陵`
- `现货双面革安妮森林;35x160-1张, 40x160-1张`（多尺寸自动拆分，共享材质/花型）

### 材质自动映射

客服手写材质名称 → 系统标准化编码：

```
双面芊/双面皮革/双面革 → 双面格
pu皮革/PU皮革       → pu防水
镜面革/镜面         → 镜面皮革
真有机硅            → 有机硅
...
```

### 赠品编码规则

| 赠品关键词 | model_code | picture_code |
|---|---|---|
| 圆垫 | 赠品沥水垫小圆或小方 | 赠品沥水垫小圆或小方 |
| 方垫 / 30x50 | 30x50 | 随机发；30x50 |
| 其他（默认） | 赠品沥水垫小圆或小方 | 赠品沥水垫小圆或小方 |

赠品材质统一为 `吸水皮革`，颜色为 `标准`。

**多赠品支持**：备注可同时包含多个赠品，如"送圆垫-2张，方垫-1张"，系统会创建对应数量的独立赠品行。

**"赠品不送"处理**：备注含"赠品不送"时，原有赠品行编码自动改为 `定制-定制-补差价-不打印`。

### 安全跳过关键字

备注包含以下关键字时自动跳过处理：

```
待定、待确定、待确认、等通知发、需要效果图、效果图、
确认再生产、号发货、安排发货、在安排发货、
已代益、已代森、仓库发、工厂发、补发
```

## 重复商品行清理（V2.2 新增）

### 问题背景

在订单修改过程中，可能因多次运行、新建行匹配错误等原因产生重复商品行。V2.2 引入全局重复清理机制：

### 清理规则

1. **扫描范围**：所有非取消（`cancelStatus=False`）的商品行
2. **分组键**：标准化 SKU + 数量
   - 标准化：忽略 `cm`/`CM`/`厘米` 单位差异
   - 忽略尾部备注差异（如"裁剪图"）
3. **保留策略**：每组仅保留一行，其余标记为取消（`cancelStatus=True`）
4. **触发时机**：更新编码前自动执行

### 典型场景

| 场景 | 处理前 | 处理后 |
|---|---|---|
| price=0 正常商品被误判为赠品 | 4行重复商品 | 保留1行，取消3行 |
| 多次运行创建重复手工单 | 2行相同 SKU+数量 | 保留1行，取消1行 |
| CM 单位差异导致重复 | `100x150CM` 与 `100x150` | 视为相同，保留1行 |

## 补差价订单处理

### 不发货触发条件

| 条件 | 处理方式 |
|---|---|
| 备注为空 | 编码改为"定制-定制-补差价-不打印"，数量改为1 |
| 备注="补差价" 或 "差价"（完全匹配） | 编码改为"定制-定制-补差价-不打印"，数量改为1 |
| 备注含"差价不发货" | 编码改为"定制-定制-补差价-不打印"，数量改为1 |
| 备注含"不打印" | 编码改为"定制-定制-补差价-不打印"，数量改为1 |
| 备注含"不用发" | 编码改为"定制-定制-补差价-不打印"，数量改为1 |
| 备注含定制信息 | 按正常解析逻辑处理，覆盖编码和数量 |

### 混合订单

普通商品行 + 补差价行共存时：
- 解析结果优先分配给普通商品行
- 剩余解析结果分配给补差价行（覆盖编码+数量）
- 无剩余时补差价行仅修改数量为1

## 快递自动配置

### 优先级

1. **备注关键词**（最高） — 备注含"发极兔"、"发中通"等关键词时，直接匹配对应快递
2. **省份规则**（次之） — 根据收件人省份匹配默认快递

### 默认省份规则

| 快递 | 省份 |
|---|---|
| 极兔 | 江苏、浙江、上海、安徽、江西、山东、福建、广东、北京、天津、湖南、湖北、河南、河北、重庆、四川、山西、陕西、广西、贵州、云南、黑龙江、吉林、辽宁 |
| 中通 | 海南、甘肃、内蒙古、宁夏、青海、西藏、新疆 |

### 快递编码映射

| 快递 | ERP编码 |
|---|---|
| 极兔 | HTKY |
| 中通 | ZTO |
| 申通 | STO |
| 顺丰 | SF |
| 圆通 | YTO |
| 韵达 | YD |

配置支持 GUI 可视化编辑，修改后实时生效。

## 自动监控模式

自适应间隔轮询功能：

| 参数 | 值 | 说明 |
|---|---|---|
| 有订单时间隔 | 10秒 | 快速处理积压订单 |
| 初始间隔 | 30秒 | 启动后首次检查间隔 |
| 无订单最大间隔 | 180秒（3分钟） | 无订单时逐步拉长 |
| 间隔放大倍数 | 1.5x | 每次无订单时间隔乘以1.5 |

特性：
- 启动时检测已登录状态，自动询问是否开启监控
- 监控中 Token 过期自动暂停并弹出登录框
- 随时可点击「停止监控」关闭
- 与手动审单互斥（手动审单时监控等待）

## 登录与 Token 管理

### 登录方式

- **推荐方式**：通过 GUI 的「登录」按钮，输入手机号密码，程序自动完成二次租户登录
- **手动方式**：技术人员在 `token.json` 中填入 Token 信息

### 登录流程

```
输入手机号+密码
  │
  ├─ 调用 /auth/login 获取初始 Token
  ├─ 调用 /get-permission-info 查找店铺端(type=1) tenant_id
  ├─ 调用 /auth/login/tenant 进行二次租户登录
  └─ 保存最终 Token 到 token.json
```

### Token 文件（`token.json`）

```json
{
  "authorization": "Bearer xxxxx",
  "cookie_str": "JSESSIONID=xxxxx",
  "tenant_id": "3005247",
  "expires_at": "2026-07-18",
  "username": "188xxxx6178",
  "_tips": "===== 此文件由程序自动管理，无需手动修改 ====="
}
```

### Token 生命周期管理

- 自动检测 Token 是否过期（每60秒检查一次）
- 过期前1天提前提醒
- 过期后自动弹出登录框
- 登录成功后自动热加载新 Token（`reload_auth()`）
- 支持退出登录（清除 Token）

## 快速开始

### 环境要求

- Python 3.9+
- 依赖库：`requests`、`urllib3`

### 安装

```bash
# 创建虚拟环境（推荐）
python -m venv .venv

# 激活虚拟环境
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 运行

**方式一：图形界面（推荐）**

```bash
python run_gui.py
```

首次使用点击「登录」，输入方果 ERP 的手机号密码，程序会自动获取 Token 并保存。登录成功后可选择开启自动监控。

**方式二：命令行**

```bash
python main.py
```

### 配置说明

主要配置在 `adapters/fangguo/config.py` 中：

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `DRY_RUN` | `False` | 设为 `True` 时仅打印不提交，安全测试用 |
| `MAX_ORDERS` | `0` | 最大处理订单数，`0` 表示不限 |
| `MATERIAL_MAP` | （内置映射表） | 材质同义词映射，可自行扩展 |
| `QUERY_STATUS` | `1` | 查询状态，`1`=待整理 |
| `TIME_BEGIN/END` | 最近7天 | 自动计算滚动时间范围 |

## 扩展其它 ERP

如需接入其他 ERP 系统，只需：

1. 在 `adapters/` 下创建新包（如 `jingdong/`）
2. 实现 `ErpAdapter` 接口（`query_orders` + `update_merchant_code`）
3. 可选：实现材质数据源以提供 `material_matcher`
4. 可选：实现 `update_express` 以支持快递更新
5. 引擎和解析器无需改动

## 测试

```bash
# 运行全部测试
pytest Test/

# 运行单个测试
pytest Test/test_stock_goods.py -v
```

测试覆盖：赠品提取、赠品格式化、现货商品、补差价订单、多尺寸拆分、合并订单、到货返过滤、编码去重、重复订单修复、手工单类型等场景。

## 技术栈

- **Python 3** — 主语言
- **requests** — HTTP 客户端（带重试机制，3次重试+退避）
- **tkinter** — 桌面 GUI（美化版，卡片布局+日志着色）
- **difflib.SequenceMatcher** — 材质模糊匹配
- **正则表达式** — 备注解析核心（尺寸/数量/花型/赠品提取）
- **JSON** — 配置持久化（Token、快递规则）

## 版本历史

### V2.2
- **新增全局重复商品行清理** — 按标准化 SKU+数量 分组，自动取消重复行
- **修复 price=0 商品误识别为赠品** — 通过 SKU 格式校验（3+ `-` 分隔符）避免误判
- **新增 SKU 模糊匹配** — CM 单位差异、尾部备注差异时自动最佳匹配
- **新增多赠品支持** — 备注可同时包含多个赠品，按子订单独立处理
- **新增商品行/赠品行去重** — 同一子订单内合并数量，跨子订单独立保留
- **新增混合订单补差价处理** — 普通行与补差价行共存时自动分配解析结果
- **新增"赠品不送"支持** — 自动将赠品行编码改为不发货编码
- **补差价关键词扩展** — 支持"差价"、"不打印"、"不用发"
- **新建手工单可删除** — type=1 显示"删除商品"按钮
- **合并订单检测顺序优化** — 先合并订单，后补差价订单
- **快递更新优化** — 仅不符规则时修改，增强编码字段兼容
- **GUI 日志精简** — 移除敏感信息和冗余数据结构
- **二次运行校验增强** — SKU+数量双重校验
- **现货多尺寸共享材质/花型** — 多尺寸现货组自动继承

### V2.1
- 新增自动监控模式（自适应间隔轮询）
- 新增快递自动配置（备注关键词 + 省份规则）
- 新增快递配置可视化编辑对话框
- 新增 Token 过期自动检测与弹窗提醒
- 新增退出登录功能
- 补差价订单支持混合订单场景
- 合并订单支持商品行级备注
- 赠品编码支持圆垫/方垫/默认三种规格
- 现货编码格式（材质-标准-尺寸-花型;尺寸，无CM单位）
- 多尺寸拆分支持材质和花型继承

### V2.0
- Token 自动登录（账号密码 → 自动获取）
- GUI 美化版（卡片布局、日志着色、确认弹窗）
- 合并订单拆分处理
- 赠品检测与编码生成
- 材质自动匹配（API + 静态映射 + 启发式）

### V1.0
- 基础审单功能
- 备注解析引擎
- 方果 ERP 适配器
- Dry Run 模式

## 许可证

本项目仅供内部使用。
