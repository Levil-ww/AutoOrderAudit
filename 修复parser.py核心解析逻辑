2026.7.4 --
一、修复parser.py的核心解析逻辑：
    问题1：裁剪有图/裁剪无图的处理
    - 在 parse_remark 、 extract_multiple_remarks 和 _parse_multi_size_direct 函数中添加了裁剪类型检测
    - 当备注包含"裁剪有图"或"裁剪无图"时，将 model_code 设置为对应值（而非"定制尺寸"）
    - picture_code 格式： 花型名;尺寸+裁剪类型+额外备注
    问题2：花型名称提取
    - 修改了花型名称提取逻辑，从分号前的文本中提取（去掉材质后）
    - 在 _extract_pattern 函数中排除了"裁剪有图"/"裁剪无图"作为花型关键词
    问题3：cm后备注内容提取
    - 修改了正则表达式，提取cm后的所有内容（包含裁剪类型和额外备注）
    - 去掉数量标记（如"-1张"），但保留其他备注内容
    问题4：修复了一个bug
    - 情况1（已经是编码格式）中， color_code 和 model_code 被错误赋值为列表而非字符串
    问题5：数量提取正则bug
    - 修改了 _RE_QTY 正则表达式，避免从尺寸（如60x150）中错误匹配数量

二、测试结果
测试用例                  备注示例                                               预期编码                                        实际结果 
裁剪有图        |    定制吸水皮革白色大理石6号;60x161cm裁剪有图-1张     |      吸水皮革-定制-裁剪有图-白色大理石6号;60x161裁剪有图  |   ✅ PASS 
裁剪无图        |    定制吸水皮革浅灰;50x80cm裁剪无图-2张              |     吸水皮革-定制-裁剪无图-浅灰;50x80裁剪无图            |   ✅ PASS 
普通定制        |    定制吸水皮革克罗印花;56x160cm-1张                 |     吸水皮革-定制-定制尺寸-克罗印花;56x160              |   ✅ PASS 
含额外备注      |    定制双面格青禾手记;31.7x46.2cm裁剪有图-1张，       |     双面格-定制-裁剪有图-青禾手记;31.7x46.2裁剪有图，    | 
               |    效果图发给顾客确认下在安排做                       |     效果图发给顾客确认下在安排做                        |   ✅ PASS 
标准订单        |    吸水皮革-标准-60x150-克罗印花;60x150              |     吸水皮革-标准-60x150-克罗印花;60x150               |   ✅ PASS 
多尺寸+裁剪有图  |    定制吸水皮革白色大理石6号;60x161cm裁剪有图-1张，     |                                                      |
              |    50x80cm裁剪有图-2张                             |      两个尺寸分别处理                                  |   ✅ PASS

现在"裁剪有图"订单的处理逻辑已正确实现， model_code 会根据备注内容设置为"裁剪有图"或"裁剪无图"， picture_code 会包含完整的尺寸和备注信息

继续修复 --
    ERP系统中，只有定制尺寸和裁剪有图，没有裁剪无图的选项，导致订单处理失败
    -> 将"裁剪无图"映射为"定制尺寸"（ERP系统支持的选项），但在 picture_code 中保留原始的"裁剪无图"信息


2026.7.06 -- 修复输出新编码的提取方式
1.添加圆形尺寸正则：新增 _RE_ROUND_SIZE 支持 圆直径80cm 格式
2.修复定制前缀检测：将 text.startswith("定制") 改为在整个文本中搜索"定制"关键字，处理"等通知发 定制..."这种情况
3.更新尺寸提取函数：- _extract_all_sizes 支持圆形尺寸
                - 新增 _extract_all_sizes_with_position 保留尺寸位置信息
4.新增材质提取函数： _extract_material_info 提取材质信息
5.重构extract_multiple_remarks：按尺寸位置分段解析，每个尺寸独立提取前后描述
6.新增辅助函数：- _extract_size_specific_remark ：提取单个尺寸的前后描述
            - _extract_global_remark_after_sizes ：提取逗号后的全局备注
7. 修复花型提取：去掉残留的"定制"前缀
8.修复尺寸格式：圆形尺寸显示为"圆直径80cm"
9.修复数量标记去除：增强正则表达式匹配不同类型的连字符

2026.7.07 -- 新编码的生成核心逻辑
最终生成的编码格式为：材质-标准/定制-尺寸/定制尺寸 or 裁剪有图-花型，实际尺寸
@property
def shop_mapping_sku(self) -> str:
    parts = [self.material_code, self.color_code,
             self.model_code, self.picture_code]
    return "-".join(parts)

一、核心流程解析
    1.入口函数 -- extract_multiple_remarks() (第205行) 处理多商品场景，而 parse_remark() (第88行) 处理单个商品。
    2.解析步骤
        步骤      函数                                   说明 
        1   检查是否已为编码格式     检测 - 分隔符，如果已有标准编码格式则直接拆分 
        2   判断是否定制             搜索 定制 关键字，确定 color_code 为 "定制" 或 "标准" 
        3   提取尺寸                _extract_all_sizes() 使用正则匹配 100x100cm 、 圆直径80cm 等 
        4   提取材质                _parse_material() 依次尝试：API自动匹配 → 静态映射表 → 启发式识别（含"革"/"格"） 
        5   提取花型                _extract_pattern() 查找花型关键词（印花、烫画等）或分号前的文本 
        6   提取裁剪类型            识别 裁剪有图 / 裁剪无图 ，映射到 model_code 
        7   提取尺寸后备注          remark_after_size 提取 cm 后面的额外说明 
        8   组装编码                根据定制/标准模式，组合各字段生成最终编码
    3.关键正则表达式
    _RE_SIZE = re.compile(r"(\d+(?:\.\d+)?)\s*[xX×*]\s*(\d+(?:\.\d+)?)\s*(?:cm|CM|厘米)?")
    _RE_ROUND_SIZE = re.compile(r"圆(?:直径)?\s*(\d+(?:\.\d+)?)\s*(?:cm|CM|厘米)?")
    _RE_QTY = re.compile(r"[-*×](\d+)\s*(?:张|个|件|套|米)(?!\s*x|X|×|\*)")
    4.多商品分割逻辑
        _split_into_segments() ( 第768行 ) 处理多尺寸/多花型场景：
        1. 多个"定制"关键词 ：按 定制 位置分割，每个 “定制...” 开头为独立商品段
        2. 单个"定制"但多个尺寸 ：分析每个尺寸前的花型名
        - 如果花型名不同 → 每个尺寸是独立商品（不同花型）
        - 如果花型名相同 → 同花型的不同尺寸
        3. 赠品处理 ：跳过以 送 开头的赠品段
        4. 共享尾部备注 ：提取所有尺寸后的公共备注（如"效果图发给顾客确认下"）
    5.字段映射关系
        字段                 定制模式                      标准模式 
        material_code    |   提取的材质名             |    提取的材质名 
        color_code       |   "定制"                  |    "标准" 
        model_code       |   "裁剪有图" / "定制尺寸"  |    实际尺寸（如 "55x140CM" ） 
        picture_code     |   花型;实际尺寸+备注       |    花型;实际尺寸+备注

修复一、
1. 优先按 "-N张，" 模式分割记录（最关键的修复） -- 修改了 _split_into_segments 函数，优先使用 -N张， 、 -N张, 、 -N张 （空格）作为记录分隔符，这是最明确的记录分隔符。
2. 花型名继承机制
为后续段补充花型名：
    - 如果段中包含分号，说明有独立花型名（如图二中的"莫比之窗"）
    - 如果段中不包含分号，继承第一个段的花型名（如图一中的"无尽夏"）
3. 尺寸前缀保留 -- 修改了 parse_remark 函数，在提取尺寸时保留尺寸前面的描述文本（如"竖版53x60"中的"竖版"）。
4. 数量信息保留
修复了数量丢失的bug：
    - 修改分割逻辑返回 (segment_text, quantity) 元组
    - 在 extract_multiple_remarks 中使用分割时提取的数量
5. 赠品过滤 -- 过滤掉以"送"开头的赠品段，避免误解析赠品为正式商品。
6. 尾部共享备注提取 -- 提取最后一个尺寸之后的共享备注，并追加到所有记录的 picture_code 中。
    修复后的解析器能够正确处理卖家备注中的多记录场景，按照"-N张，"作为分隔符进行分割，并正确提取每个记录的花型名、尺寸描述和数量信息。

修复二、
多场景实现后，单记录场景出现识别不到问题
    问题原因 ：在 _split_into_segments 函数的 else 分支（没有找到 -N张， 分割点的情况）中，正则表达式缺少捕获组，导致 qty_match.group(1) 抛出 IndexError 。
修复方案 ：
    1. 修复正则表达式 （第927行）：将 r"-\d+[张个件套米]" 改为 r"-(\d+)[张个件套米]" ，添加捕获组来提取数量数字
    2. 修复数量提取 （第1027-1028行和第1096-1097行）：在 else 分支中使用 _extract_qty() 函数提取数量，而不是硬编码为1

修复三、
构建订单商品行，程序使用的是原始订单中的商品数量，而不是通过解析出的数量
修复内容
    问题原因 ：在 adapter.py 的 _build_order_item 函数中，商品数量使用的是原始订单中的数量（ item.num ），而不是从卖家备注中解析出的数量（ parsed.num ）。
    修复方案 ：将第494行的 "num": item.num 改为 "num": parsed.num 。

修复四、
取消：“共，共计x张”追加到picture_code中
针对备注 定制吸水皮革摩登空间;13x82.5cm-1张，13x87cm-1张，共计2张 ，程序现在可以：
正确解析结果：
    商品行              SKU                             数量 
    1       吸水皮革-定制-定制尺寸-摩登空间;13x82.5CM       1 
    2       吸水皮革-定制-定制尺寸-摩登空间;13x87CM         1
修复的问题
    1. 数量汇总信息过滤 （ parser.py ）：过滤掉"共计N张"这种数量汇总信息，不将其追加到picture_code中
    2. 交叉乘积bug修复 （ adapter.py ）：将嵌套循环改为1:1映射，每个解析结果对应一个商品行，而不是创建N×M个商品行
    3. 数量传递 （ adapter.py ）：使用解析出的数量 parsed.num 而不是原始订单数量 item.num
完整流程
    1. 解析卖家备注，按 -N张， 分割成独立记录
    2. 每个记录提取花型名（如"摩登空间"）和尺寸（如"13x82.5CM"）
    3. 过滤掉"共计N张"等数量汇总信息
    4. 为每个记录创建独立的商品行，数量使用解析出的值
    5. 调用ERP接口更新商家编码



自动审单跳过场景：
程序中跳过自动审单的条件主要分布在两个层面：
一、引擎层跳过条件（engine.py）
        条件     代码位置              说明
    卖家备注为空 第78-81行 当 order.shop_remark 为空或只包含空白字符时，跳过该订单 
    无法解析备注 第92-95行 当 extract_multiple_remarks() 返回空列表时，说明无法从备注中提取有效商品信息

二、解析器层过滤条件（parser.py）
        条件            代码位置                             说明 
    赠品段跳过      第858-859行                 以"送"开头的商品段（如"送沥水垫25cm-1张"）被直接跳过，不参与解析 
    无尺寸段过滤    第861-864行                 不包含尺寸信息（如 100x100cm ）的段不被视为有效商品，归入潜在尾部备注 
    数量汇总过滤    第314-315行、第895-896行    "共计N张"、"共三张"等数量汇总文本被过滤掉，不加入编码 
    空备注直接返回   第270-271行                空文本直接返回空结果列表


完整跳过流程
查询订单 → 检查备注是否为空 → 是 → 跳过
         ↓ 否
     解析备注 → 分割商品段 → 过滤赠品段 → 过滤无尺寸段 → 提取尾部备注
         ↓
     检查解析结果是否为空 → 是 → 跳过
         ↓ 否
     正常处理订单
典型跳过场景
    1. 卖家备注为空或空白 ：订单直接跳过
    2. 备注无法解析 ：如完全不符合格式的乱码文本
    3. 仅包含赠品 ：如"送沥水垫25cm-1张"，没有主商品
    4. 缺少尺寸信息 ：无法确定规格的商品

程序的自动审单，是不是对解码成功和解码失败都能够审单？？
      场景        ERP系统状态         程序处理                    结果
    备注格式正确     解码成功         解析成功，提交审单           ✅ 审单成功
    备注格式正确     解码失败         解析成功，提交审单           ✅ 审单成功（覆盖系统的解码失败）
    备注格式错误     解码失败         解析失败，跳过              ⏭️ 跳过
    备注含关键字     任意状态         关键字检查在解析之前，直接跳过 ⏭️ 跳过
结论：程序会尝试解析所有订单的备注（无论ERP系统显示什么状态），只要备注格式正确就能审单成功。 程序的解析逻辑和ERP系统的解码逻辑是独立的两套系统


修复五、
EG：用户的备注"定制 定制吸水皮革摩登空间;"没有尺寸信息，但却被成功解析了 -- 涉及数据一致性和约束锁定
问题根源 -- 用户的备注"定制 定制吸水皮革摩登空间;"没有尺寸信息，但仍然被成功解析了
原因是：
    1. parse_remark 调用 _parse_simple （ parser.py ）：当没有尺寸时，代码调用简单模式解析
    2. _parse_simple 只检查材质 （ parser.py ）：只要提取到材质就标记为成功，不检查尺寸
    3. 引擎认为解析成功 ：因为 parsed.success=True ，所以不会跳过订单
修复方案 -- 修改 _parse_simple 函数，在没有尺寸信息时不标记为成功，这样没有尺寸的订单就会被正确跳过。

我发现还有一个问题。在 extract_multiple_remarks 函数中（ parser.py ），条件是：
    if parsed.success or (parsed.material_code and parsed.picture_code):
即使 parsed.success 为 False，只要有 material_code 和 picture_code ，结果也会被添加。对于没有尺寸的备注， picture_code 会被设置为 "定制;定制尺寸" ，所以这个条件仍然会满足。
我需要修改这个条件，确保只有真正有尺寸信息的订单才会被添加：
- 移除 _parse_simple 中的成功标记 ：删除了 if result.material_code: result.success = True ，这样没有尺寸的简单模式解析不会被标记为成功
- 加强 extract_multiple_remarks 的添加条件 ：新增 has_real_size 判断，确保只有包含真实尺寸信息（如 50x50cm ）的解析结果才会被添加到列表中

修复六、
问题分析：卖家备注中的"共计X张"、"共X张"等数量汇总信息被错误地追加到新编码中。根本原因是原来的正则表达式只匹配阿拉伯数字（ \d+ ），无法匹配中文数字（如"两"、"三"）。
修复方案：
    1.新增正则表达式：
    _CHINESE_NUMBERS = r"(?:[一二两三四五六七八九十]|\d+)"
    _RE_QTY_SUMMARY = re.compile(rf"共(?:计)?{_CHINESE_NUMBERS}[张个件套米]")
    2.更新的函数
        2.1. parse_remark - 两处 remark_after_size 提取逻辑
        2.2. extract_multiple_remarks - 尾部备注追加逻辑
        2.3. _remove_sizes_and_qty - 尺寸和数量移除逻辑
        2.4. _extract_common_pattern - 花型提取逻辑
        2.5. _split_into_segments - 两处尾部备注提取逻辑
        2.6. _clean_segment - 商品段清理逻辑

2026.7.08
修复七、赠品问题
1、存在一些问题需要修复：
    1.花型字段末尾包含了多余的"赠"字
    2.如果订单中已经有赠品行，当前代码会重复添加
    3.需要实现对已有赠品行数量的修改 / 或者没有赠品行，需要生成一个
2、赠品商品行处理
    如果订单中已经有赠品行 （如图一中的第二个商品行"赠品沥水垫小圆或小方"）：
    - ✅ 系统会自动检测到该赠品行
    - ✅ 将数量从原来的 1 修改为备注中指定的 2
    如果订单中没有赠品行 ：
    - ✅ 系统会自动生成一个新的赠品行
    - ✅ 数量设置为备注中指定的数量（如 2 ）
3、我做的修复
我修复了以下几个问题：
    1. 花型字段末尾多余的"赠"字 - 在 parser.py 中修复了尾部备注提取逻辑，避免"赠送"被错误拆分
    2. 赠品行数量更新 - 在 adapter.py 中添加了 _handle_gift_item 方法：
        - 检测已有赠品行并更新数量
        - 克隆原始商品行数据，保留身份字段（id/sysOid等）
        - 使用模糊匹配（检测SKU或标题中包含"赠品"）
    3. 赠品名称优化 - 去掉多余的"赠品"前缀，使提取更准确
    4. 保留未被替换的商品行 - 确保订单中其他商品行不会被意外删除

修复九、
1、新增确认回调机制
    新增参数 confirm_callback，签名：
    confirm_callback(order, parsed_list, changes: list[str]) -> bool
    - 在 _process_order 中，解析成功且非 dry_run 时，先调确认回调
    - 回调返回 True → 继续执行 update_merchant_code API 修改
    - 回调返回 False → 仅日志记录，不调 API（统计到 cancelled 中）
    - 不传回调时行为不变（旧版兼容）
2、gui - 弹窗交互
    在 AutoAuditGUI 中新增：
    要素	                                说明
    _batch_confirm_decision	    缓存用户对本次批量的决定，避免每个订单都弹窗
    _confirm_update()	        线程安全的弹窗方法，用 root.after(0, ...) 调度到主线程显示
3、运行流程
    点击「开始审单」
  ↓
查询待处理订单
  ↓
逐一解析备注 → 打印新编码
  ↓
  ┌─ DRY_RUN=True → 仅打印，不弹窗，不改
  │
  └─ DRY_RUN=False → 弹出确认框：
     ┌──────────────────────────┐
     │  订单 xxx 备注解析成功    │
     │  • 新编码: XX-YY-ZZ-WW   │
     │                          │
     │  是否一键自动审单？       │
     │  ───────────────         │
     │  确定→自动更新所有订单    │
     │  取消→仅日志，不修改      │
     │                          │
     │       [确定]  [取消]      │
     └──────────────────────────┘
       │
       ├─ 确定 → 调用 saveProduct API → ✅ 修改成功
       │         （后续订单自动沿用"确定"，不再重复弹窗）
       │
       └─ 取消 → ⏭️ 用户取消，仅记录日志
                 （后续订单自动沿用"取消"）
关键行为：
    1.弹窗只弹出一次，你的选择会应用到本次批量所有订单
    2.config.py 中 DRY_RUN = True 时不会弹窗（模拟模式不调 API）
    3.DRY_RUN = False 时才弹窗确认，给你一次反悔的机会


2026.7.09
修复十、
问题1：编码末尾包含"共/共计X张"，且"裁剪图一张"错误附加到所有行
    根因：extract_multiple_remarks 中 trailing_remark（尾部备注）被附加到了每一个商品行，而实际上它只应属于最后一个商品行。
    同时"共计2张"的剥离正则写错了（\\d写成双反斜杠导致失效）。
    修复（parser.py）：
        trailing_remark 只附加到 idx == len(segments) - 1 最后一条
        修复了正则 \d（原来是 \\d 导致不匹配）
        增加了第二层剥离保护

修复十一、
在备注里包含赠品信息，但尾部还有其他信息时，会添加到商品编码中去
修复1.赠品名称提取修复
    修复了 _extract_gift 函数：提取赠品数量后，只保留数量标记之前的内容，不再包含"不要有误差"等后续备注
修复2.分割逻辑修复
    修复了 _split_into_segments 函数：当检测到赠品关键词时，跳过该分割点及之后的所有内容，防止赠品后面的备注被误附加到商品行
修复3.赠品行编码固定
    修改了 _build_gift_item 函数：赠品行使用固定编码"吸水皮革-标准-赠品沥水垫小圆或小方-赠品沥水垫小圆或小方“
修复4.赠品行更新修复
    修改了 _handle_gift_item 函数：更新现有赠品行时，不仅更新数量，还覆盖所有礼品编码字段（ filmGiftCode 、 giftCodeName 、 pictureCode 、 shopMappingSku 等），确保不会有商品编码泄漏到赠品中
修复5.中文数字支持
    添加了中文数字（一到十）的支持，如"送沥水垫两张"可以正确提取数量为2

2026.7.10
修复十二、
问题：解析备注时，如果没有材质也要跳过，不处理（原存在没有尺寸会跳过）如“定制无痕止滑松灰色13#;49x153cm剪裁有图-1张”，就没有包含材质“有机硅”，
但系统还是将其进行了识别为可审单
修复- 所有正常备注都能正确解析，没有材质的备注会被正确跳过
    - parser.py - 修复 API 自动匹配部分：当去掉"定制"前缀后代码为空时，继续尝试其他匹配方式
    - parser.py - 修复静态映射表匹配部分：同样的逻辑修复
    - parser.py - 添加安全检查：确保没有材质的订单不会被添加到结果中
    - config.py - 移除误导性条目：删除 "定制": "定制"

修复十三、
对审单出错的订单，如重复生成，或因分割不准编码多余，进行强制修改，覆盖
    1. 修复 _extract_pattern 函数 [core/parser.py:811-L835]
    在提取花型名称前先过滤赠品信息，避免赠品名称（如"小垫子"）被误识别为花型。

    2. 修复 parse_remark 函数中的 remark_after_size 提取逻辑 [core/parser.py:199-L232]
    完善赠品信息的过滤逻辑，不仅在"送"等关键词处截断，还向前找到分隔符，把"发小垫子总共送3个"中的"小垫子"也一并去掉。

    3. 修复 _extract_all_sizes 和 _extract_all_sizes_with_position 函数 [core/parser.py:395-L448]
    在提取尺寸前先找到赠品信息的起始位置，只提取赠品之前的尺寸，避免赠品尺寸被误提取。

    4. 修复 _split_into_segments 函数的分割模式 [core/parser.py:1062-L1064]
    修改分割正则表达式，支持更多分隔符模式：
        - 原来：只匹配 -N张， （必须有"-"前缀）
        - 现在：匹配 -N张， 、 N张， 、 -N张 （空格）等

修复十四、
对于合并订单中子订单的赠品创建问题
样例：合并（6954358067748672&6954358067748672）的订单，实现了分组解决，但订单号为“6954358067748672742”的备注为“定制吸水皮革摩登空间;40x160cm-1张，送赠品一张”，在程序中运行好像没有实现赠品行的创建
但当前系统存在：只保留匹配的商品行、赠品行和作废行，但合并订单中被跳过的子订单的商品行（非作废行、非赠品、未匹配）会被删除
修复内容：
    1.修复赠品提取逻辑（parser.py）
    问题 ：当"送"在文本开头时， prev_char 是空字符串 "" ，而空字符串是任何字符串的子字符串，导致被误判为"发送达放"中的字符，直接返回空结果。
    2.修复赠品名称处理（parser.py）
    问题 ：当赠品名称就是"赠品"时，去掉前2个字符后变成空字符串。
    3.修复未匹配商品行被删除的问题（adapter.py）
    问题 ：合并订单中被跳过的子订单的商品行（非作废、非赠品、未匹配）会被删除。
    修复 ：在第三步增加保留未匹配的非作废、非赠品商品行的逻辑。

赠品行的创建条件
只有满足以下三个条件时才会创建赠品行：
    1. gift_name 不为空（如"赠品"、"防滑垫"等）
    2. gift_num > 0（数量大于0）
    3. material_code 不为空（有材质信息）
在您的场景中：
- gift_name = "赠品"
- gift_num = 1
- material_code = "吸水皮革"
三个条件都满足，所以会创建一个数量为1的赠品行。


